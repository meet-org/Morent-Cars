import React, { Component } from 'react';
import "./header.scss"

class Header extends Component {
  render() {
    return (
      <header>
        
      </header>
    )
  }
}

export default Header;